dist: trusty
sudo: false
language: java
services:
  - docker
before_script:
  - docker login -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"
script:
  - ./gradlew build
  - ./gradlew dockerPush -DbuildNumber="$TRAVIS_BUILD_NUMBER"
